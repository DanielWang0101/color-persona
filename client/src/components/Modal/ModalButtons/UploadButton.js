import React from "react";

const UploadButton = () => {
  return (
    <div>
      {/* <button
      onClick={(ev) => {
        ev.preventDefault();
        handleSharePalette();
      }}
    >
      Share
    </button>  */}
    </div>
  );
};

export default UploadButton;
